{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Injector GUI",
            "command": ".\\build.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
